NODE_WS_URL=ws://127.0.0.1:9944
DATABASE_URL=postgres://postgres:password@localhost/polkadot_clob

# Logging configuration (optional, defaults to "info")
# Examples:
#   RUST_LOG=debug                           # All modules at debug level
#   RUST_LOG=indexer=debug                   # Only indexer crate at debug level
#   RUST_LOG=indexer=trace,info              # Indexer at trace, everything else at info
#   RUST_LOG=indexer::api::websocket=trace   # Only websocket module at trace level
RUST_LOG=indexer=debug,info